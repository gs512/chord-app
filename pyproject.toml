[project]
name = "chord-app"
version = "0.1.0"
description = "Chord voicing visualization and interval analysis"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.30.0",
    "matplotlib>=3.8.0",
]

[project.scripts]
chord-app = "streamlit.web.cli:main"
